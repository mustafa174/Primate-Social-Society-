import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme, props) => ({
	root: {
		width: '98%',
		margin: 'auto'
	},
	bannerImg: {
		height: '100%',
		width: '100%'
	}
}));
